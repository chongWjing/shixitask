<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huashan.backend.mapper.OrdersMapper">

    <resultMap id="BaseResultMap" type="com.huashan.backend.entity.Orders">
            <id property="id" column="id" />
            <result property="orderNo" column="order_no" />
            <result property="userId" column="user_id" />
            <result property="productId" column="product_id" />
            <result property="productName" column="product_name" />
            <result property="productImage" column="product_image" />
            <result property="productPrice" column="product_price" />
            <result property="quantity" column="quantity" />
            <result property="totalPrice" column="total_price" />
            <result property="receiverName" column="receiver_name" />
            <result property="receiverPhone" column="receiver_phone" />
            <result property="receiverAddress" column="receiver_address" />
            <result property="status" column="status" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="Base_Column_List">
        id,order_no,user_id,product_id,product_name,product_image,
        product_price,quantity,total_price,receiver_name,receiver_phone,
        receiver_address,status,create_time,update_time
    </sql>
<!--    创建订单-->
    <insert id="insertOrders" useGeneratedKeys="true" keyProperty="id">
        insert into orders(order_no,user_id,product_id,product_name,product_image,
        product_price,quantity,total_price,receiver_name,receiver_phone,
        receiver_address,status)
        values(#{orderNo},#{userId},#{productId},#{productName},#{productImage},
        #{productPrice},#{quantity},#{totalPrice},#{receiverName},#{receiverPhone},
        #{receiverAddress},#{status})
    </insert>

<!--    查询订单-->
    <select id="findById" resultType="com.huashan.backend.entity.Orders">
        select <include refid="Base_Column_List"/> from orders where id=#{id}
    </select>
<!--    查询用户订单-->
    <select id="findByUserId" resultType="com.huashan.backend.entity.Orders">
        select <include refid="Base_Column_List"/> from orders where user_id=#{userId} order by create_time desc
    </select>
<!--    更新订单状态-->
    <update id="updateStatus">
        update orders set status=#{status},update_time=NOW() where id=#{id}
    </update>
</mapper>
