# 前端开发文档 - 项目搭建与页面开发

> **核心流程**：Vite 创建 Vue3 项目 → Figma API 生成页面骨架 → 前端 axios 封装 → 接入后端 API
> **前置条件**：后端 API 已就绪（参考后端开发文档）

---

## 时间安排

| 时间 | 阶段 | Step | 内容 | 预览效果 |
|------|------|------|------|---------|
| 9:00-9:10 | 准备 | Step1 | Vite 创建 Vue3 项目+配置 | 项目能启动 |
| 9:10-9:20 | 准备 | Step2 | Figma 设计稿准备+PAT 获取+API 验证 | API 能拉取 Figma 数据 |
| 9:20-9:30 | 前端封装 | Step3 | 前端 axios 统一封装 | 编译通过 |
| 9:30-9:35 | 前端封装 | Step4 | 前端 API 模块封装 | 编译通过 |
| 9:35-10:05 | **Figma API 生成** | Step5 | **Figma API 生成 Login.vue 骨架** | 看到完整登录页 |
| 10:05-10:25 | **Figma API 生成** | Step6 | **Figma API 生成 Home.vue 骨架** | 看到完整商城首页 |
| 10:25-10:35 | 路由更新 | Step7 | 更新路由配置（添加 ShopLayout） | 首页能访问 |
| 10:35-10:50 | 联调 | Step8 | **Login + Home 接入真实数据** | 真实登录走通 |

---

## Step1（9:00-9:10）Vite 创建 Vue3 项目

### 操作

在 `E:\HuaShan\shoping` 目录下：

```bash
npm create vite frontend -- --template vue
cd frontend
npm install
npm install vue-router axios element-plus
npm install -D unplugin-vue-components unplugin-auto-import unplugin-element-plus
```

#### vite.config.js

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import ElementPlus from 'unplugin-element-plus/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'

export default defineConfig({
  plugins: [
    vue(),
    AutoImport({ imports: ['vue', 'vue-router'] }),
    Components({ resolvers: [ElementPlusResolver()] }),
    ElementPlus()
  ],
  server: {
    port: 5173,
    proxy: { '/api': { target: 'http://localhost:8080', changeOrigin: true } }
  }
})
```

#### main.js + App.vue

```javascript
// main.js
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'
import 'element-plus/dist/index.css'
import { ElMessage, ElMessageBox } from 'element-plus'
const app = createApp(App)
app.use(router)
app.config.globalProperties.$message = ElMessage
app.config.globalProperties.$confirm = ElMessageBox.confirm
app.mount('#app')
```

```vue
<!-- App.vue -->
<template><router-view /></template>
<script>export default { name: 'App' }</script>
<style>body { margin:0; padding:0; background:#f5f5f5; }</style>
```

#### 路由骨架 router/index.js

**Step1 初始版本**（空路由，后续步骤逐步添加）：

```javascript
import { createRouter, createWebHistory } from 'vue-router'

const routes = [
  // 默认重定向到登录页（后续会改为首页）
  { path: '/', redirect: '/shop/login' }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

export default router
```

> **注意**：此时路由为空，Login.vue 在 Step5 创建后再添加路由。

### ✅ 预览效果

`npm run dev` → 访问 `http://localhost:5173` → 项目能启动。

---

## Step2（9:10-9:20）Figma 设计稿准备+PAT 获取+API 验证

### 操作

#### 2.1 准备 Figma 设计稿

**方式A：直接在 Figma 中设计**（如果有设计师）

**方式B：用 html.to.design 插件从 HTML 原型转换（推荐）**

1. 在 Figma 中安装插件 `html.to.design`
2. 点击插件中的 "Install browser extension" 安装浏览器扩展
3. 浏览器打开本地 HTML 原型（已在 `E:\HuaShan\shoping\原型图\` 中）：
   - `http://localhost:8899/shop-login.html`
   - `http://localhost:8899/shop-home.html`
4. 点击扩展图标 → 捕获页面 → 自动转为 Figma 可编辑图层
5. 7 个页面逐一操作，全部导入 Figma 项目

#### 2.2 生成 Figma 个人访问令牌（PAT）

这是唯一需要手动操作的步骤：

1. 打开 Figma 首页 → 点击右上角头像 → **Settings（设置）**
2. 左侧滚动到 **Personal access tokens** 区域
3. 点击 **Generate new token**
4. **Description** 随便写（例如 `vue-generator`）
5. **Scopes** 只勾 `File content - Read only`，别开写权限
6. 点击 **Generate token**
7. **立刻复制保存**——只显示一次，关掉就找不到

> 形如：`figd_你的Figma个人访问令牌`

#### 2.3 解析 Figma 链接，提取关键参数

从 Figma 中复制设计稿链接，典型长这样：

```
https://www.figma.com/design/YhwSLSOJ43A2HTeaVGCaWO/shopping?node-id=14-92&t=KvA6OVdYokw3ciN5-4
```

| 参数 | 位置 | 本例取值 | 说明 |
|------|------|---------|------|
| `FILE_KEY` | URL 路径第 3 段 | `YhwSLSOJ43A2HTeaVGCaWO` | 设计文档唯一 ID |
| `NODE_ID` | `node-id=` 查询参数 | `14-92` | 要还原的节点 ID |

> ⚠️ API 要求 node-id 写成 `14:92`（冒号分隔），URL 里是 `14-92`（横杠），**记得替换**。

#### 2.4 通过 Figma API 验证连接

在终端中执行（Windows PowerShell 注意用 `curl.exe`）：

```powershell
curl.exe -s -H "X-FIGMA-TOKEN: 你的PAT" ^
  "https://api.figma.com/v1/files/FILE_KEY/nodes?ids=NODE_ID" ^
  -o figma_node.json
```

**成功标志**：命令立即返回，没有报错，当前目录下出现 `figma_node.json` 文件（几十 KB 到几百 KB）。

**常见报错与解决**：

| 报错 | 原因 | 解决 |
|------|------|------|
| `Invoke-WebRequest` 一串红色 | 用了 `curl` 而非 `curl.exe` | PowerShell 下必须写完整 `curl.exe` |
| `{"err":"403"}` | PAT 过期或无权限 | 回 2.2 重新生成；确认设计稿对令牌可访问 |
| `{"err":"404"}` | FILE_KEY / NODE_ID 写错 | 回到 2.3 重新核对链接 |
| `{"err":"429"}` | 请求太频繁 | 等 1 分钟再试 |

### ✅ 预览效果

- Figma 中有 7 个商城页面的设计稿
- 终端通过 Figma API 能拉取设计稿的 JSON 数据（图层结构、样式、组件信息）
- `figma_node.json` 文件成功生成，包含所有设计规格

> 📖 完整教程参考：[Figma转Vue完整教程.md](./Figma转Vue完整教程.md)

---

## Step3（9:20-9:30）前端 axios 统一封装

### 背景

前端所有接口调用都走同一个 axios 实例，这样：
1. 统一 baseURL + vite dev proxy
2. 请求拦截器自动附带 `Authorization: Bearer <token>`
3. 响应拦截器统一按 `{ code, message, data }` 拆包，401 自动清 token + 跳登录页

### 操作

创建 `src/api/request.js`：

```javascript
import axios from 'axios'
import { ElMessage } from 'element-plus'

const request = axios.create({
  baseURL: '/api',
  timeout: 10000
})

// 响应拦截器：统一处理错误
request.interceptors.response.use(
  response => {
    const { code, message } = response.data
    if (code !== 200) {
      ElMessage.error(message || '请求失败')
      return Promise.reject(new Error(message || '请求失败'))
    }
    return response.data
  },
  error => {
    const message = error.response?.data?.message || error.message || '网络错误'
    ElMessage.error(message)
    return Promise.reject(error)
  }
)

export default request
```

> **Vite dev proxy 配套**：`vite.config.js` 已经配了 `'/api' -> 'http://localhost:8080'`，所以前端写 `request.post('/user/login')`，浏览器实际发的就是 `http://localhost:5173/user/login`，由 Vite 代理到后端 `http://localhost:8080/user/login`，**完美解决 5173→8080 的跨域问题**。

### ✅ 预览效果

- 编译通过，无报错

---

### ⚠️ 路径前缀规则（强制执行）

> `baseURL` 已设为 `/api`，**所有 `api/*.js` 模块中的路径禁止再加 `/api` 前缀**。

```javascript
// ✅ 正确
request.get('/product/list')       // → /api/product/list
request.post('/user/login')        // → /api/user/login

// ❌ 错误（会导致 /api/api/product/list → 404）
request.get('/api/product/list')
request.post('/api/user/login')
```

---

## Step4（9:30-9:35）前端 API 模块封装

### 背景

将用户相关的 API 调用抽离到独立模块 `src/api/user.js`，便于维护和复用。

### 操作

创建 `src/api/user.js`：

```javascript
import request from './request'

/**
 * 用户登录
 * @param {Object} data - { username, password }
 */
export function login(data) {
  return request.post('/user/login', data)
}

/**
 * 用户注册
 * @param {Object} data - { username, password, phone }
 */
export function register(data) {
  return request.post('/user/register', data)
}

/**
 * 获取当前登录用户信息
 */
export function getUserInfo() {
  return request.get('/user/info')
}

/**
 * 退出登录
 */
export function logout() {
  return request.post('/user/logout')
}

/**
 * 更新用户资料（手机号）
 * @param {Object} data - { phone }
 */
export function updateProfile(data) {
  return request.put('/user/profile', data)
}

/**
 * 修改密码
 * @param {Object} data - { oldPassword, newPassword }
 */
export function changePassword(data) {
  return request.put('/user/password', data)
}
```

### ✅ 预览效果

- 编译通过，无报错
- Login.vue 中可通过 `import { login, register } from '../../api/user'` 引入

---

### ⚠️ API 函数完整性校验

> 后续创建的任何新组件（Home.vue、ProductDetail.vue 等）如果需要调用后端接口，必须在对应的 `api/*.js` 模块中补充 `export function`。
> **严禁** .vue 组件 import 了但 api 模块未导出的函数。

---

## Step5（9:35-10:05）Figma API 生成 Login.vue 骨架

### 操作

#### 5.1 在 Figma 中获取登录页节点数据

1. 打开 Figma 项目 → 找到登录页设计稿
2. 选中整个页面 Frame → 右键 → Copy link
3. 从链接中提取 `FILE_KEY` 和 `NODE_ID`（参见 Step2.3）

#### 5.2 通过 Figma API 拉取节点 JSON

```powershell
curl.exe -s -H "X-FIGMA-TOKEN: 你的PAT" ^
  "https://api.figma.com/v1/files/FILE_KEY/nodes?ids=NODE_ID" ^
  -o login_node.json
```

#### 5.3 阅读 JSON，提取设计规格

遍历 `login_node.json`，提取关键样式数据：

| Figma 属性 | 提取方式 | Vue/CSS 对应 |
|------------|---------|-------------|
| `fills[].gradientStops` | `r/g/b` × 255 转 hex | `linear-gradient(135deg, #FFE0E6, #C71526)` |
| `absoluteBoundingBox` | 取 width/height | `width: 420px; height: 576px` |
| `style.fontFamily/Size/Weight` | 直接取值 | `font-size: 32px; font-weight: 700` |
| `cornerRadius` | 直接取值 | `border-radius: 16px` |
| `effects[].DROP_SHADOW` | offset + radius + color | `box-shadow: 0 0 12px rgba(0,0,0,0.12)` |
| `layoutMode: VERTICAL` | 自动布局方向 | `flex-direction: column; gap: 18px` |
| `strokes[]` + `strokeWeight` | 描边颜色和粗细 | `border: 1px solid #E4E7ED` |

提取出的设计规格清单：
```
根容器 1920×1208 渐变背景
 ├── 🌸🌹🌷💐 装饰 emoji，opacity=0.3，四角分布
 └── 白色卡片 420×576（水平居中）
      ├── 登录 | 注册 Tab（16px）
      ├── 用户名 Label + 输入框 354×40（#DCDFE6 边框 4px 圆角）
      ├── 密码 Label + 输入框 同上
      ├── 红色按钮 354×40（#C71526 16px 白色字 letterSpacing=4）
      └── 底部装饰线
```

#### 5.4 完整提示词（可直接复制使用）

将以下提示词复制给 AI，即可生成完整的登录页面（视觉 + 功能）：

```
请创建商城登录/注册页面 src/views/shop/Login.vue。

## 一、视觉层（参考 Figma 设计稿）

Figma 信息：
- PageLink: https://www.figma.com/design/AFiGtQvehsPAVKkx63vYCm/LoginPage?node-id=2-117&t=B9qRVtHUkormLgSo-4
- PAT: figd_你的Figma个人访问令牌

通过 Figma API 拉取设计稿 JSON

从 JSON 中提取以下视觉规格并 1:1 还原：

1. 页面整体（**必须铺满全屏，这是独立页面，不是嵌套在 Layout 中**）：
   - 渐变背景：linear-gradient(135deg, #FFE0E6 0%, #FFB6C1 30%, #C71526 100%)
   - **CSS 关键：`.login-page` 必须 `min-height: 100vh; width: 100%;`，不能减去任何值**
   - 四角分布装饰花朵 emoji（🌸🌹🌷💐），opacity=0.3，带浮动动画
   - 内容水平垂直居中（flex align-items:center; justify-content:center）

2. Logo 区域（卡片上方）：
   - 🌸 图标 56px，带 drop-shadow
   - 标题"花之恋"：32px，白色，bold，letter-spacing=4px
   - 副标题"每一束花，都是爱的告白"：14px，白色半透明

3. 白色卡片：
   - 宽度 max-width: 420px，圆角 16px，shadow="always"
   - 内边距 28px 32px 16px

4. Tab 切换：
   - 登录 | 注册 两个 Tab，各占 50% 宽度
   - 激活色 #C71526

5. 表单输入框：
   - 用户名：prefix-icon="User"，size="large"
   - 密码：prefix-icon="Lock"，size="large"，show-password
   - 注册额外增加手机号：prefix-icon="Phone"，size="large"

6. 提交按钮：
   - type="danger"，size="large"，宽度 100%
   - 背景 #C71526，letter-spacing=4px
   - hover 色 #A01020

## 二、布局关系

- 这个页面是独立页面，路由为 /shop/login
- 不需要嵌套在 Layout.vue 中

## 三、数据层

API 从 '../../api/user' 导入，该模块已存在，提供以下函数：
- login(data) → POST /user/login，data 为 { username, password }
- register(data) → POST /user/register，data 为 { username, password, phone }

后端返回格式：
```json
// 登录成功
{ "code": 200, "message": "success", "data": { "token": "eyJ...", "user": { "id": 1, "username": "test", "role": 0, "phone": "139..." } } }
// 注册成功
{ "code": 200, "message": "success", "data": "注册成功" }
// 业务错误（如密码错误）
{ "code": 500, "message": "密码错误", "data": null }
```

⚠️ axios 拦截器返回的是整个 `{ code, message, data }` 对象，所以组件中 `const res = await login(...)` 之后：
- `res.data` 是 data 字段的内容（登录时为 `{ token, user }`，注册时为 `"注册成功"`）
- 不要用 `res.data.data` 或 `res.result` 等不存在的字段

错误由 axios 拦截器（src/api/request.js）统一处理，组件内 catch 写空块即可。

## 四、交互逻辑

1. 登录流程：
   - 点击"登录"按钮 → 先调用 el-form 的 validate() 校验
   - 校验通过 → 调用 login(this.loginForm)
   - 成功 → 存 localStorage：token、username、role（分开存三项）
   - 显示 ElMessage.success('登录成功，欢迎回来！')
   - 跳转到 this.$route.query.redirect 或 '/shop'
   - 失败 → catch 空块（拦截器已处理）

2. 注册流程：
   - 点击"注册"按钮 → 先调用 el-form 的 validate() 校验
   - 校验通过 → 调用 register(this.registerForm)
   - 成功 → ElMessage.success('注册成功，请登录')，切回登录 Tab，自动填入注册的用户名
   - 失败 → catch 空块

3. 表单验证规则：
   - 登录：用户名必填，密码必填
   - 注册：用户名必填，密码必填且不少于6位，手机号必填且正则 /^1[3-9]\d{9}$/

## 五、技术要求

- 使用 Options API 风格（export default { data(), methods }）
- <style scoped>
- 使用 Element Plus 组件：el-card、el-tabs、el-tab-pane、el-form、el-form-item、el-input、el-button、el-text、el-link
- 装饰花朵用 CSS @keyframes float 动画（6s ease-in-out infinite）

## 六、Import 依赖校验（强制执行）

> 组件中每个 `import ... from '...'` 语句必须是真实存在的：
> - `import { login, register } from '../../api/user'` → 已确认 api/user.js 中已导出
> - `import { ElMessage } from 'element-plus'` → Element Plus 已全局注册
> - 禁止 import 不存在的模块或函数
```

#### 5.5 编写 Vue 组件

将设计规格 1:1 映射为 `template + CSS + script`，保存为 `src/views/shop/Login.vue`：

- **template**：和 Figma 结构 1:1 对应，FRAME→`<div>`，TEXT→语义标签
- **CSS**：`<style scoped>`，Flexbox 优先，装饰层用 absolute 定位
- **字体栈**：`'PingFang SC', 'Microsoft YaHei', sans-serif`
- **颜色**：严格按 API 给出的 RGB 转 hex
- **注册表单字段**：用户名、密码、**手机号**（后端 `UserController.register` 要求 username/password/phone 三个参数，对应数据库 user 表 phone 字段）
- **表单验证**：使用 Element Plus 的 `el-form` 组件自带的 `rules` 验证规则

#### 5.6 Login.vue 完整实现（Options API + 表单验证）

```vue
<!-- 商城登录/注册页 - 鲜花电商"花之恋"主题 -->
<template>
  <div class="login-page">
    <div class="login-bg-decoration">
      <span class="deco-flower deco-1">🌸</span>
      <span class="deco-flower deco-2">🌹</span>
      <span class="deco-flower deco-3">🌷</span>
      <span class="deco-flower deco-4">💐</span>
    </div>
    <div class="login-container">
      <!-- Logo区域 -->
      <div class="login-logo">
        <span class="logo-icon">🌸</span>
        <h2 class="login-title">花之恋</h2>
        <p class="login-subtitle">每一束花，都是爱的告白</p>
      </div>

      <!-- 登录/注册卡片 -->
      <el-card class="login-card" shadow="always">
        <el-tabs v-model="activeTab" class="login-tabs">
          <!-- 登录Tab -->
          <el-tab-pane label="登录" name="login">
            <el-form
              ref="loginFormRef"
              :model="loginForm"
              :rules="loginRules"
              label-position="top"
              class="login-form"
            >
              <el-form-item label="用户名" prop="username">
                <el-input
                  v-model="loginForm.username"
                  placeholder="请输入用户名"
                  prefix-icon="User"
                  size="large"
                />
              </el-form-item>
              <el-form-item label="密码" prop="password">
                <el-input
                  v-model="loginForm.password"
                  type="password"
                  placeholder="请输入密码"
                  prefix-icon="Lock"
                  size="large"
                  show-password
                />
              </el-form-item>
              <el-form-item>
                <el-button
                  type="danger"
                  size="large"
                  class="submit-btn"
                  :loading="loginLoading"
                  @click="onLogin"
                >
                  登 录
                </el-button>
              </el-form-item>
            </el-form>
          </el-tab-pane>

          <!-- 注册Tab -->
          <el-tab-pane label="注册" name="register">
            <el-form
              ref="registerFormRef"
              :model="registerForm"
              :rules="registerRules"
              label-position="top"
              class="login-form"
            >
              <el-form-item label="用户名" prop="username">
                <el-input
                  v-model="registerForm.username"
                  placeholder="请输入用户名"
                  prefix-icon="User"
                  size="large"
                />
              </el-form-item>
              <el-form-item label="密码" prop="password">
                <el-input
                  v-model="registerForm.password"
                  type="password"
                  placeholder="请输入密码（至少6位）"
                  prefix-icon="Lock"
                  size="large"
                  show-password
                />
              </el-form-item>
              <el-form-item label="手机号" prop="phone">
                <el-input
                  v-model="registerForm.phone"
                  placeholder="请输入手机号"
                  prefix-icon="Phone"
                  size="large"
                />
              </el-form-item>
              <el-form-item>
                <el-button
                  type="danger"
                  size="large"
                  class="submit-btn"
                  :loading="registerLoading"
                  @click="onRegister"
                >
                  注 册
                </el-button>
              </el-form-item>
            </el-form>
          </el-tab-pane>
        </el-tabs>
      </el-card>
    </div>
  </div>
</template>

<script>
import { login, register } from '../../api/user'
import { ElMessage } from 'element-plus'

export default {
  name: 'ShopLogin',
  data() {
    return {
      activeTab: 'login',
      loginLoading: false,
      registerLoading: false,
      /* 登录表单 */
      loginForm: {
        username: '',
        password: ''
      },
      /* 注册表单 */
      registerForm: {
        username: '',
        password: '',
        phone: ''
      },
      /* 登录表单验证规则 */
      loginRules: {
        username: [{ required: true, message: '请输入用户名', trigger: 'blur' }],
        password: [{ required: true, message: '请输入密码', trigger: 'blur' }]
      },
      /* 注册表单验证规则 */
      registerRules: {
        username: [{ required: true, message: '请输入用户名', trigger: 'blur' }],
        password: [
          { required: true, message: '请输入密码', trigger: 'blur' },
          { min: 6, message: '密码不少于6位', trigger: 'blur' }
        ],
        phone: [
          { required: true, message: '请输入手机号', trigger: 'blur' },
          { pattern: /^1[3-9]\d{9}$/, message: '手机号格式不正确', trigger: 'blur' }
        ]
      }
    }
  },
  methods: {
    /** 登录 */
    async onLogin() {
      try {
        await this.$refs.loginFormRef.validate()
      } catch {
        return
      }
      this.loginLoading = true
      try {
        const res = await login(this.loginForm)
        if (res.data && res.data.token) {
          const user = res.data.user || {}
          localStorage.setItem('token', res.data.token)
          localStorage.setItem('username', user.username || this.loginForm.username)
          localStorage.setItem('role', String(user.role != null ? user.role : 0))
          ElMessage.success('登录成功，欢迎回来！')
          // 跳转到登录前页面，或首页
          const redirect = this.$route.query.redirect
          this.$router.push(redirect || '/shop')
        }
      } catch (e) {
        // 错误由拦截器处理
      } finally {
        this.loginLoading = false
      }
    },
    /** 注册 */
    async onRegister() {
      try {
        await this.$refs.registerFormRef.validate()
      } catch {
        return
      }
      this.registerLoading = true
      try {
        await register(this.registerForm)
        ElMessage.success('注册成功，请登录')
        this.activeTab = 'login'
        this.loginForm.username = this.registerForm.username
        this.loginForm.password = ''
      } catch (e) {
        // 错误由拦截器处理
      } finally {
        this.registerLoading = false
      }
    }
  }
}
</script>

<style scoped>
.login-page {
  min-height: 100vh;
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #FFE0E6 0%, #FFB6C1 30%, #C71526 100%);
  padding: 40px 20px;
  position: relative;
  overflow: hidden;
}

/* 背景装饰花朵 */
.login-bg-decoration {
  position: absolute;
  width: 100%;
  height: 100%;
  top: 0;
  left: 0;
  pointer-events: none;
}

.deco-flower {
  position: absolute;
  font-size: 48px;
  opacity: 0.3;
  animation: float 6s ease-in-out infinite;
}

.deco-1 { top: 10%; left: 8%; animation-delay: 0s; }
.deco-2 { top: 60%; left: 5%; animation-delay: 2s; font-size: 64px; }
.deco-3 { top: 15%; right: 10%; animation-delay: 1s; font-size: 56px; }
.deco-4 { bottom: 15%; right: 8%; animation-delay: 3s; font-size: 40px; }

@keyframes float {
  0%, 100% { transform: translateY(0) rotate(0deg); }
  50% { transform: translateY(-20px) rotate(10deg); }
}

.login-container {
  width: 100%;
  max-width: 420px;
  position: relative;
  z-index: 1;
}

/* Logo区域 */
.login-logo {
  text-align: center;
  margin-bottom: 28px;
}

.logo-icon {
  font-size: 56px;
  display: inline-block;
  filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.2));
}

.login-title {
  margin: 12px 0 6px;
  font-size: 32px;
  color: #fff;
  font-weight: bold;
  letter-spacing: 4px;
  text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
}

.login-subtitle {
  color: rgba(255, 255, 255, 0.85);
  font-size: 14px;
  margin: 0;
}

/* 登录卡片 */
.login-card {
  border-radius: 16px;
}

.login-card :deep(.el-card__body) {
  padding: 28px 32px 16px;
}

.login-tabs :deep(.el-tabs__nav) {
  width: 100%;
}

.login-tabs :deep(.el-tabs__item) {
  width: 50%;
  text-align: center;
  font-size: 16px;
  font-weight: 500;
}

.login-tabs :deep(.el-tabs__active-bar) {
  background-color: #C71526;
}

.login-tabs :deep(.el-tabs__item.is-active) {
  color: #C71526;
}

.login-form {
  margin-top: 12px;
}

.submit-btn {
  width: 100%;
  margin-top: 8px;
  background: #C71526;
  border-color: #C71526;
  font-size: 16px;
  letter-spacing: 4px;
}

.submit-btn:hover {
  background: #A01020;
  border-color: #A01020;
}
</style>
```

后端接口字段对照：

| 后端接口 | HTTP | 前端 request body | 后端接收字段 |
|---------|------|-------------------|------------|
| `/user/login` | POST | `{ username, password }` | `Map<String,String>` |
| `/user/register` | POST | `{ username, password, phone }` | `Map<String,String>` |

### ✅ 预览效果

访问 `http://localhost:5173/shop/login`：
- ✅ **完整登录页布局**（红色渐变背景+花之恋品牌+装饰花朵）
- ✅ 登录/注册双 Tab 切换
- ✅ 注册表单多了「手机号」输入框

---

## Step6（10:05-10:25）Figma API 生成 Home.vue 骨架

### 操作

#### 6.1 获取商城首页节点数据

1. 打开 Figma → 找到商城首页设计稿（包含导航栏+分类+Hero+Footer）
2. 选中整个页面 Frame → Copy link
3. 提取 `FILE_KEY` 和 `NODE_ID`

#### 6.2 通过 Figma API 拉取节点 JSON

```powershell
curl.exe -s -H "X-FIGMA-TOKEN: 你的PAT" ^
  "https://api.figma.com/v1/files/FILE_KEY/nodes?ids=NODE_ID" ^
  -o home_node.json
```

#### 6.3 提取设计规格 + 编写 Vue 组件

按照 Step5 相同的方法，从 `home_node.json` 提取导航栏、搜索框、Hero 轮播、分类栏、商品列表、评价区、Footer 等样式规格，1:1 映射为 Vue 组件。

> ⚠️ **注意**：本文档的 Step6 生成的是初步的 Home 骨架。**完整首页请参考《前端开发文档-商城首页功能实现.md》**，该文档使用 ShopLayout 布局 + 嵌套 Home.vue 内容区的架构，并包含完整的数据接入逻辑。

#### 6.4 手动调整：登录态 + 退出登录

Home 顶部公告栏和头部导航**根据登录状态动态切换显示**：

```javascript
<script>
import { logout } from '../../api/user'
import { ElMessage } from 'element-plus'

export default {
  name: 'ShopHome',
  data() {
    return {
      isLoggedIn: false,
      username: ''
    }
  },
  mounted() {
    this.loadUser()
  },
  methods: {
    loadUser() {
      const token = localStorage.getItem('token')
      const username = localStorage.getItem('username')
      if (token) {
        this.isLoggedIn = true
        this.username = username || '用户'
      } else {
        this.isLoggedIn = false
        this.username = ''
      }
    },
    goLogin() {
      this.$router.push({ path: '/shop/login', query: { redirect: '/shop' } })
    },
    async handleLogout() {
      try {
        await logout()
      } catch (e) {
        // 忽略错误，继续清理本地状态
      }
      localStorage.removeItem('token')
      localStorage.removeItem('username')
      localStorage.removeItem('role')
      this.isLoggedIn = false
      this.username = ''
      ElMessage.success('已退出登录')
      this.goLogin()
    }
  }
}
</script>
```

template 中顶栏用户区：
```html
<!-- 顶部公告栏 user-area -->
<div class="user-area" v-if="isLoggedIn">
  <span>Hi, {{ username }}</span>
  <span class="divider">|</span>
  <a class="logout-link" @click="handleLogout">退出</a>
</div>
<div class="user-area" v-else>
  <a class="logout-link" @click="goLogin">登录</a>
  <span class="divider">|</span>
  <a class="logout-link" @click="goLogin">注册</a>
</div>
<!-- 头部导航右侧 -->
<a v-if="isLoggedIn" class="action-link user-link">👤 {{ username }}</a>
<a v-else class="action-link user-link" @click="goLogin">👤 登录</a>
```

### ✅ 预览效果

- ✅ 完整商城首页骨架生成

---

## Step7（10:25-10:35）更新路由配置（添加 ShopLayout）

### 操作

更新 `src/router/index.js`，添加 ShopLayout 和所有商城页面路由：

```javascript
import { createRouter, createWebHistory } from 'vue-router'
import { ElMessage } from 'element-plus'

// 商城页面
const ShopLayout = () => import('../views/shop/ShopLayout.vue')
const ShopHome = () => import('../views/shop/Home.vue')
const ShopLogin = () => import('../views/shop/Login.vue')
const ShopProductDetail = () => import('../views/shop/ProductDetail.vue')
const ShopOrder = () => import('../views/shop/Order.vue')
const ShopMyOrders = () => import('../views/shop/MyOrders.vue')
const ShopCart = () => import('../views/shop/Cart.vue')
const ShopUserCenter = () => import('../views/shop/UserCenter.vue')

const routes = [
  // 默认重定向到商城首页
  { path: '/', redirect: '/shop' },

  // 商城路由 - 使用ShopLayout包裹
  {
    path: '/shop',
    component: ShopLayout,
    children: [
      { path: '', name: 'ShopHome', component: ShopHome },
      { path: 'login', name: 'ShopLogin', component: ShopLogin },
      { path: 'product/:id', name: 'ShopProductDetail', component: ShopProductDetail },
      { path: 'order/:id', name: 'ShopOrder', component: ShopOrder, meta: { requiresAuth: true } },
      { path: 'cart', name: 'ShopCart', component: ShopCart, meta: { requiresAuth: true } },
      { path: 'user', name: 'ShopUserCenter', component: ShopUserCenter, meta: { requiresAuth: true } },
      { path: 'my-orders', name: 'ShopMyOrders', component: ShopMyOrders, meta: { requiresAuth: true } }
    ]
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

// 全局路由守卫
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('token')

  // 商城页面需要登录（检查所有匹配路由的meta）
  const requiresAuth = to.matched.some(record => record.meta.requiresAuth)
  if (requiresAuth && !token) {
    // 友好提示：告诉用户需要先登录
    ElMessage.warning('请先登录后再访问该页面')
    next({ path: '/shop/login', query: { redirect: to.fullPath } })
    return
  }

  next()
})

export default router
```

### ✅ 预览效果

访问 `http://localhost:5173/shop`：
- ✅ **完整商城首页**：顶栏公告 + Logo 导航 + 搜索框 + Hero 轮播 + 分类 + 商品 + 评价 + Footer
- ✅ 未登录时右上角显示「登录 | 注册」
- ✅ 登录后自动刷新为「Hi, <用户名> | 退出」

---

## Step8（10:35-10:50）Login + Home 接入真实数据

### 整体调用链

```
┌──── Login.vue ────┐        ┌──── api/user.js ────┐        ┌──── 后端 /api/user ────┐
│  import { login }  │   →   │  login(data) {      │   →   │  UserController        │
│  await login(form) │  ←    │    post('/user/ │  ←    │  → UserServiceImpl      │
│                    │       │      login', data)  │       │    MD5Util + JwtUtil   │
└────────────────────┘       └─────────────────────┘       └────────────────────────┘
         │                                  │                                  │
         │ 前端 localStorage 落地           │ axios 拦截器处理                 │ 数据库 INSERT/SELECT
         ▼                                  ▼                                  ▼
  token / username / role 持久化      自动带 Bearer token              user 表 password MD5 存储
         │
         ▼
  router.push('/shop')
         │
         ▼
  ┌──── Home.vue ────┐
  │ onMounted → loadUser()│  读 localStorage.username
  │ 顶栏: Hi, <username> │
  │ 退出 → logout()       │  调 POST /user/logout 清 token
  └──────────────────────┘
```

### Login.vue 登录 / 注册（完整代码见 Step5.6）

登录：
```javascript
import { login } from '../../api/user'

async onLogin() {
  await this.$refs.loginFormRef.validate()
  const res = await login(this.loginForm)
  if (res.data && res.data.token) {
    const user = res.data.user || {}
    localStorage.setItem('token', res.data.token)
    localStorage.setItem('username', user.username || this.loginForm.username)
    localStorage.setItem('role', String(user.role != null ? user.role : 0))
    ElMessage.success('登录成功，欢迎回来！')
    const redirect = this.$route.query.redirect
    this.$router.push(redirect || '/shop')
  }
}
```

注册：
```javascript
import { register } from '../../api/user'

async onRegister() {
  await this.$refs.registerFormRef.validate()
  await register(this.registerForm)
  ElMessage.success('注册成功，请登录')
  this.activeTab = 'login'
  this.loginForm.username = this.registerForm.username
  this.loginForm.password = ''
}
```

### Home.vue 顶栏动态切换（完整代码见 Step6.4）

后端接口总览：

| 方法 | 路径 | 需登录 | 前端 |
|------|------|--------|------|
| POST | `/user/register` | ❌ | Login 注册 |
| POST | `/user/login` | ❌ | Login 登录 |
| POST | `/user/logout` | ✅ | Home 退出 |
| GET  | `/user/info`   | ✅ | （后续）个人中心 |

### ✅ 联调预览效果

**真实联调走通**：
- ✅ 注册新用户 → 数据库 `user` 表新增一行（password 是 MD5 后的 32 位）
- ✅ 登录 → 后端返回真实 JWT（以 `Bearer ` 存入 localStorage）
- ✅ 登录后跳转 `/shop` → Home 顶栏显示「Hi, <用户名>」
- ✅ 退出登录 → 清 token → 跳登录页
- ✅ 后端返回 "密码错误" / "用户名已存在" 时，前端 ElMessage 直接弹出
- ✅ 401（token 过期）自动清 token + 跳登录页

---

## 总结

### 完成清单

- [x] Vite 创建 Vue3 项目
- [x] **Figma 设计稿准备+PAT 获取+API 验证**
- [x] 前端 axios 封装
- [x] 前端 API 模块封装
- [x] **Figma API 生成 Login.vue 骨架**（立刻看到登录页）
- [x] **Figma API 生成 Home.vue 骨架**（立刻看到完整商城首页）
- [x] 更新路由配置（添加 ShopLayout）
- [x] **Login + Home 接入真实数据**

### 预览效果总结

| 效果 | 状态 |
|------|------|
| Figma API 验证成功 | ✅ |
| Figma API 生成登录页（立刻看到效果） | ✅ |
| Figma API 生成商城首页（立刻看到效果） | ✅ |
| 真实注册登录走通 | ✅ |
| 未登录跳转登录页 | ✅ |

### Figma API 价值体现

| 传统方式 | Figma API 方式 |
|---------|---------------|
| 手写 HTML+CSS，30-40min/页面 | API 拉取 JSON+规格映射，5-10min/页面 |
| 学生看着代码，兴趣不高 | 学生看到设计稿数据直接变页面，兴趣高涨 |
| 布局样式需反复调试 | API 精确给出尺寸/颜色/间距，像素级还原 |

### 面试要点

1. **Figma API -> Vue 原理**：通过 Figma REST API 拉取设计稿 JSON，递归遍历节点树提取 fills/style/boundingBox 等属性，1:1 映射为 Vue template + CSS
2. **JWT vs Session**：JWT 无状态适合分布式
3. **CORS 跨域**：前端 5173->后端 8080 跨域，**Vite dev proxy 一层搞定**（本项目方案），也可后端配 allowedOriginPatterns
4. **axios 封装要点**：baseURL、request interceptor 自动带 token、response interceptor 统一错误处理
5. **localStorage 存储**：token、username、role 分开存储，便于读取和清理
6. **路由守卫**：beforeEach 全局守卫检查 meta.requiresAuth，实现权限控制
